function calculateGrade() {
    let studentScore = "100";
    let grade;

    if (studentScorecore > 79) {
      grade = "A";
    } else if (studentScore >= 60) {
      grade = "B";
    } else if (studentScore >= 50) {
      grade = "C";
    } else if (studentScore >= 40) {
      grade = "D";
    } else {
      grade = "E";
    }
